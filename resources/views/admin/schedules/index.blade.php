@extends('layouts.app')@section('title','Jadwal Pelajaran')@section('content')<div class="page-head"><div><h1>Jadwal Pelajaran</h1><p>Relasi guru, kelas, dan mata pelajaran.</p></div><a class="btn primary" href="{{ route('admin.schedules.create') }}">+ Tambah Jadwal</a></div><div class="card table-wrap"><table><thead><tr><th>Hari/Jam</th><th>Guru</th><th>Kelas</th><th>Mapel</th><th>Ruang</th><th>Aksi</th></tr></thead><tbody>@foreach($items as $x)<tr><td>{{ $x->day_of_week }}<br><small>{{ substr($x->start_time,0,5) }}–{{ substr($x->end_time,0,5) }}</small></td><td>{{ $x->teacher->user->name }}</td><td>{{ $x->schoolClass->name }}</td><td>{{ $x->subject->name }}</td><td>{{ $x->room }}</td><td class="actions"><a class="btn sm" href="{{ route('admin.schedules.edit',$x) }}">Edit</a><form method="post" action="{{ route('admin.schedules.destroy',$x) }}">@csrf @method('delete')<button class="btn sm danger">Hapus</button></form></td></tr>@endforeach</tbody></table></div>{{ $items->links() }}@endsection
