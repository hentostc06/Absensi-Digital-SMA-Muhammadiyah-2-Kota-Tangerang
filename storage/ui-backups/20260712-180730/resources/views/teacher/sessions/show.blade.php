@extends('layouts.app')@section('title','QR Absensi Dinamis')@section('content')<div class="page-head"><div><h1>{{ $session->subject->name }} — {{ $session->schoolClass->name }}</h1><p>QR Code berubah otomatis setiap 30 detik.</p></div>@if($session->isOpen())<form method="post" action="{{ route('teacher.sessions.close',$session) }}">@csrf<button class="btn danger">Tutup Akses</button></form>@endif</div><div class="qr-layout"><div class="card qr-card"><div id="qr-status" class="badge green">Sesi Aktif</div><img id="qr-image" alt="QR Code dinamis"><div class="countdown"><span>Kode diperbarui dalam</span><strong id="countdown">30</strong><span>detik</span></div><small>Waktu server: <span id="server-time">-</span></small></div><div class="card"><div class="page-head"><h3>Kehadiran Real-time</h3><span class="badge green"><b id="attendance-count">{{ $session->attendances->count() }}</b> siswa</span></div><div class="table-wrap"><table><thead><tr><th>NIS</th><th>Nama</th><th>Status</th><th>Waktu</th></tr></thead><tbody id="attendance-list">@foreach($session->attendances as $a)<tr><td>{{ $a->student->nis }}</td><td>{{ $a->student->user->name }}</td><td>{{ ucfirst($a->status) }}</td><td>{{ $a->scanned_at?->format('H:i:s') }}</td></tr>@endforeach</tbody></table></div></div></div>@if($session->isOpen())@push('scripts')<script>dynamicQr({tokenUrl:@json(route('teacher.sessions.token',$session)),attendanceUrl:@json(route('teacher.sessions.attendance',$session))});</script>@endpush @endif@endsection
