@extends('layouts.app')@section('title','Mata Pelajaran')@section('content')<div class="page-head"><div><h1>Mata Pelajaran</h1><p>Kelola katalog mapel.</p></div><a class="btn primary" href="{{ route('admin.subjects.create') }}">+ Tambah Mapel</a></div><div class="card table-wrap"><table><thead><tr><th>Kode</th><th>Nama</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@foreach($items as $x)<tr><td>{{ $x->code }}</td><td>{{ $x->name }}</td><td>{{ $x->is_active?'Aktif':'Nonaktif' }}</td><td class="actions"><a class="btn sm" href="{{ route('admin.subjects.edit',$x) }}">Edit</a><form method="post" action="{{ route('admin.subjects.destroy',$x) }}">@csrf @method('delete')<button class="btn sm danger">Hapus</button></form></td></tr>@endforeach</tbody></table></div>{{ $items->links() }}@endsection
