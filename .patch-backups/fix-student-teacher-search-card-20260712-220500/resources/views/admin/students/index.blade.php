@extends('layouts.app')@section('title','Data Siswa')@section('content')<div class="page-head"><div><h1>Data Siswa</h1><p>Kelola akun dan identitas siswa.</p></div><a class="btn primary" href="{{ route('admin.students.create') }}">+ Tambah Siswa</a></div><form class="filters"><input name="q" value="{{ request('q') }}" placeholder="Cari NIS atau nama..."><button class="btn">Cari</button></form><div class="card table-wrap"><table><thead><tr><th>NIS</th><th>Nama</th><th>Kelas</th><th>Status Akun</th><th>Aksi</th></tr></thead><tbody>@forelse($items as $x)<tr><td>{{ $x->nis }}</td><td>{{ $x->user->name }}</td><td>{{ $x->schoolClass->name }}</td><td><span class="badge {{ $x->user->is_active?'green':'red' }}">{{ $x->user->is_active?'Aktif':'Nonaktif' }}</span></td><td class="actions"><a class="btn sm" href="{{ route('admin.students.edit',$x) }}">Edit</a><form method="post" action="{{ route('admin.students.destroy',$x) }}" onsubmit="return confirm('Hapus siswa ini?')">@csrf @method('delete')<button class="btn sm danger">Hapus</button></form></td></tr>@empty<tr><td colspan="5" class="empty">Belum ada data.</td></tr>@endforelse</tbody></table></div>{{ $items->links('vendor.pagination.badcoding') }}@endsection
