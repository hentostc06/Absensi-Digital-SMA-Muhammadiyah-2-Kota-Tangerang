@extends('layouts.app')@section('title','Sesi Absensi')@section('content')<div class="page-head"><div><h1>Sesi Absensi Guru</h1><p>Buka sesi sesuai jadwal, lalu tampilkan QR di proyektor.</p></div></div><form method="post" action="{{ route('teacher.sessions.store') }}" class="card form grid">@csrf<label class="full">Jadwal<select name="schedule_id" required><option value="">Pilih jadwal</option>@foreach($schedules as $s)<option value="{{ $s->id }}">{{ $s->day_of_week }} {{ substr($s->start_time,0,5) }} — {{ $s->subject->name }} / {{ $s->schoolClass->name }}</option>@endforeach</select></label><label>Toleransi Terlambat (menit)<input type="number" name="late_after_minutes" value="15" min="0" max="120"></label><div class="actions"><button class="btn primary">Buka Sesi & Generate QR</button></div></form><div class="card table-wrap"><h3>Riwayat Sesi</h3><table><thead><tr><th>Dibuka</th><th>Kelas</th><th>Mapel</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@foreach($sessions as $s)<tr><td>{{ $s->opened_at->format('d-m-Y H:i') }}</td><td>{{ $s->schoolClass->name }}</td><td>{{ $s->subject->name }}</td><td><span class="badge {{ $s->status==='open'?'green':'gray' }}">{{ $s->status==='open'?'Aktif':'Ditutup' }}</span></td><td><a class="btn sm" href="{{ route('teacher.sessions.show',$s) }}">Buka</a></td></tr>@endforeach</tbody></table></div>{{ $sessions->links() }}@endsection
